import InstallCommand from '../../../src/plugin/command/InstallCommand';

describe('InstallCommand test', () => {

    // TODO: test
    test('InstallCommand is instantiable', () => {
        expect(new InstallCommand()).toBeInstanceOf(InstallCommand);
    });
});
