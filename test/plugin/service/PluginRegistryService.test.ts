import { PluginRegistryService } from '../../../src/plugin/service/PluginRegistryService';

describe('ConfigurationService test', () => {

    test('PluginRegistryService is instantiable', () => {
        expect(new PluginRegistryService()).toBeInstanceOf(PluginRegistryService);
    });

    // TODO: test
});
