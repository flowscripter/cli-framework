import HelpCommand from '../../../src/core/command/HelpCommand';

describe('HelpCommand test', () => {

    // TODO: test
    test('HelpCommand is instantiable', () => {
        expect(new HelpCommand()).toBeInstanceOf(HelpCommand);
    });
});
